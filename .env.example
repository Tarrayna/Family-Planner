# Copy to .env and fill in.

# Google OAuth (Calendar sync) — from the Google Cloud Console
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Random secrets — e.g. `openssl rand -hex 32`
TOKEN_ENCRYPTION_KEY=
SESSION_SECRET=
